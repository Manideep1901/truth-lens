[build]
  command = "npm run build"
  publish = "dist"

[secrets]
  # Tell Netlify to ignore these keys in frontend bundles
  omit_keys = [
    "VITE_SUPABASE_URL",
    "VITE_SUPABASE_ANON_KEY",
    "VITE_SUPABASE_PROJECT_ID"
  ]
